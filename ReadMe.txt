“When creating a database in MySQL, run the query below first. Then, go to the scripts folder and copy each table script. Simply copy and paste the data for each table, and then execute the query in MySQL.”

-- Create database
CREATE DATABASE IF NOT EXISTS rms;
USE rms;